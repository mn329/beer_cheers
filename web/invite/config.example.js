// コピーして config.js として使う（または直下の config.js を編集する）。
window.BeerCheersInviteConfig = {
  // Firebase Hosting のホスト名（カスタムドメインがあればそれに合わせる）
  hostingHost: "beercheers.web.app",
  // Apple Developer Team ID（AASA の appID と一致させる）
  appleTeamID: "K3BSAX58ZG",
  // Bundle ID
  bundleID: "com.minato.beer-cheers",
  // App Store 公開後に数値 ID を入れる。空なら Store ボタンを出さない。
  appStoreID: "",
};
